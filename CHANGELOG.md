# Changelog

All notable changes. Dates are 2026.

## Unreleased

- **Track T — eval release gates:** `--release-gate` (`--golden-version`, `--gate-k` default 3) exits 0 only if the last k golden-answer runs clear today's `EVAL_THRESHOLD` on one judge, printing the window table either way; mixed judges, sub-threshold scores, and short windows fail with the recovery spelled out. Per-model runs never enter the window. README gains the flags plus a 3-step release checklist (verify, gate, tag — all local, CI has no GPU/Postgres). Live proof fixed a real wiring bug (gate read `--drift-golden` instead of `--golden-version`).

- **Track S — append-only transition trail:** new `audit` package + `0007_audit_log.sql` recording workflow created/resumed/step_done/completed/failed and eval started/finished as typed-constructor events (fixed fields only — a prompt structurally cannot enter the log); writers are best-effort (logged, never abort); `GET /v1/activity` reads chains. Grilled pre-code: eval history was already append-only and resume UPDATEs are operational state, so the brief descoped from "rewrite writers" to "record the trail". Live proof caught a real bug (query named `created_at`, migration names `ts` — endpoint 502'd; fixed + `TestSQLHistoryMapsRows` regression test).

- **Track R — router context budget:** machine-assembled prompts (judge context, researcher context) trim to 3000 tokens by a documented chars/4 estimate, tail-first from relevance order, never emptied; dropped counts surface in the `--score` pair log and researcher log (no span changes, no user-traffic trimming). Proven: unit red-green (estimate, tail-drop, trim-fires, no-op) + live tracker run quiet on small corpus + chat unaffected.

- **Track Q — embedding hash cache:** `--ingest` keeps SHA-256 `{hash}.json` sidecars in `evals/corpus/cache/` (git-ignored, beside the clean tree, never inside it) and re-embeds only new, cross-model, or corrupt chunks, logging `hits/new/corrupt/model_changed`. Sidecars carry the model name so a model bump misses and overwrites automatically; embed→sidecar→upsert order converges on re-run. Proven live: 36 new first run, 36 hits (seconds, zero embed calls) second run, retrieval still hits `04-metrics` first. Recovery: `rm -rf evals/corpus/cache` + `--ingest`.

- **Track P — agent-config self-scan:** `TestPolicyAgentConfigClean` fails the build on secret shapes, URL-embedded credentials, pipe-to-shell, `chmod 777`, or disabled host-key checks in shipped/executed config (agent files, workflows, compose, Dockerfile, console assets, MCP readme). Docs/lessons/tests are out of scope by construction — no allow-list to grow. It caught a real one on arrival: a `postgres://USER:PASSWORD@…` DSN literal in `mcp/README.md`, now env parts like the compose fix.

- **Track O — pre-commit verify gate:** `scripts/verify.sh` is the single definition of green (gofmt, vet, tests, build, policy, diff stat) — CI's Go job calls it with `VERIFY_RACE=1`, developers run it bare; secret scanning stays in Track P's allow-listed policy test, not in a grep. Proven red-green (scratch breakage fails gofmt, removal restores green); README documents the gate plus the manual Windows equivalent.

- **Track L — local benchmarking (code done, scoring runs in flight):** `--score --score-model NAME` generates each golden answer with the named model (temperature 0, 256 tokens, same retrieval, same judge) and tags the run; `eval_runs.model` + per-pair `latency_s`/`tokens` (`0006_benchmark.sql`); `GET /v1/benchmarks` compares latest runs per model with per-scenario splits by routing reason and a Welch-t verdict that names a winner only at n≥100 / p<0.05, else `tied — route on cost`; Tower `#/benchmarks` page (matrix, scenarios, history, four states, catalogue-only). Tests: `TestScoreGeneratedPair(+ErrorFailsLoud)`, `TestWelchPValue*`, `TestBuildComparison*`, `TestBenchmarksEndpoint`, `TestRecordAndDriftReport` (model assertion). Faithfulness math untouched (`TestScorePair` still green on the shared `judgeAnswer` path).

- **Track K — static advisor (ADR-0012):** `go run . --advise` prints a startup report per configured model — size, quant, context note, license, VRAM fit against the 8 GB budget (`docs/VRAM.md`), presence from Track J, and the one-at-a-time co-residency verdict — plus a cannot-fit veto that gates Track L entry. Cached public scores ship empty with the reason printed: no Qwen2.5-3B/7B rank found today was citable enough to freeze, and `TestCachedRumorsCarryProvenance` fails the build on any future sourceless number. One flag (README row), no env vars, no endpoints, zero behaviour change; proven live (both tiers pulled/fit, unknown-presence path with Ollama down). the gateway no longer advertises models it cannot serve. `OllamaClient.PulledModels` decodes `GET /api/tags` (full name plus `:latest`-stripped); the health prober refreshes the pulled set every 15 s tick; unpulled refs are filtered before `Plan()` (unknown presence never filters, so cloud fallbacks and offline tests behave as before); explicit or tier requests for a missing model answer `503 model_not_pulled` naming the model and the fix, in the one error shape (403 still wins for sensitive-to-cloud); `GET /v1/models` shows `up:false` + `reason:"not_pulled"`; gaps log once per model at boot and on change. Also fixed live: `OLLAMA_MODELS` entries containing `/` or `\` are skipped — Ollama itself uses that variable for its models directory, which had a filesystem path advertised as a tier-`local` model. Tests: `TestOllamaPulledModelsDecodesTags`, `TestPresenceSkipsUnpulled`, `TestModelsShowsNotPulledReason`, `TestRefreshPulledLogsGapOnce`, `TestAfterProbeRunsEachTick`, `TestExtraModelsSkipsPaths`, policy `TestPolicyModelNotPulledIsOneErrorShape`; proven live (fake fast tier → 503s, pulled 7B → 200).
- **Conversations retention actually runs:** the 90-day boot purge never executed — `($1 || ' days')::interval` types the parameter as text while pgx hands it an int, so every boot logged `purge skipped`. Now `make_interval(days => $1)` (`TestPurgeConversationsBuildsTypedInterval`); proven live with a backdated thread (`dropped 1`).

- **Tower dashboard replaces Grafana (approach A, spans-only):** `#/overview` gains a *Models — requests, errors and tokens* panel (`modelsTable()` in `pageOverview`) rendering the five Grafana panels natively from spans — per-model last-hour counts (`by_model`), exact cumulative errors/tokens (`since_start`), p50/p99 from the last-30 sample (labelled as a sample, not a histogram quantile), error-rate pills (`clean` / `%` degraded / critical), faithfulness unchanged. Reuses `tower-panel/table/mono/pill/chip/empty` only — no new classes, no hex, `h()` only; example added to `docs/tower-design-system.html` (Data display). The `monitoring` compose profile and `dashboard/` (Grafana JSON, provisioning, Prometheus config) are deleted; `GET /metrics` exposition and the k6 `load-tests/router.js` gate stay untouched. `README.md` updated. Follow-up (not in this change, golden discipline): `corpus/04-metrics.md` + frozen golden pairs still describe the Grafana stack — needs corpus v4 + golden v4 + a scored run.
- **Universal MCP + live rail (Track N):** the MCP server is no longer Claude-only in docs or in reach — same five tools over stdio (`--mcp`) and over `POST /mcp` (one request per POST, same handler, Bearer key required when `REQUIRE_API_KEY=true`); every `tools/call` emits a redacted `mcp.tool` span (`tool`/`ok`/`code` only, fenced by `TestPolicyMCPToolSpansRedacted`); spans fan out to `GET /v1/events` SSE for the new Tower `#/live` ticket rail, and `#/try` sends one prompt through the chat path with a trace deep-link. No new env vars, flags, or Tower classes.
- **Cockpit (`#/cockpit`):** send + sight on one surface — sticky send/answer column beside the shared live rail, own ticket pinned amber, client-held conversation (`messages[]` resent each turn, capped at ~4000 tokens with a truncated chip; gateway stays stateless per ADR-0010), and "Run as workflow" posting the last input to the existing toy runner. Stays a nav entry.
- **Console:** `#/try` and `#/live` retired into `#/cockpit` — one surface, one live rail (`mountRail` shared, no drift); old hashes fall back to Overview.
- **Evals:** retrieval misses are no longer scored as unfaithful. `ScorePair` returns `retrieval_miss=true` with zero judge calls when `recall=0`; `eval_pair_scores.faithfulness` is nullable (`0004_retrieval_miss.sql`) with `NULL` for misses, the suite average and drift report exclude misses (`misses_now/misses_then`), and the console shows a `retrieval miss` chip with `–` for faith. History starts clean — old runs keep old scores.
- **v1.1 gateway (ADR-0009): generation parameters, client references, extra local models.** The first real multi-agent client — Versatile's LangGraph writing orchestrator, which places Writer on a GPU model and Critic/Editor on a CPU model (`num_gpu: 0`) and needs a seeded, low-temperature judge with JSON output — could not run through the gateway because v1.0 forwarded only `max_tokens` and served two model names. `POST /v1/chat/completions` now accepts `temperature`, `top_p`, `seed`, `stop`, `response_format` (OpenAI shape) and `format`, `options`, `keep_alive`, `think` (Ollama shape); a new `ParamBackend` interface (`GenerateWith` / `StreamWith` with `GenParams`) is implemented by both backends while plain `Backend`s keep getting `max_tokens` only; `X-Client-Ref` / `X-Agent-Role` headers (or body fields, bounded at 64 / 32 chars) land on all three spans and are echoed back, so a client can join its own record to the gateway's trace and the inspector can group a run by role; `OLLAMA_MODELS` registers extra local models (tier `local`) addressable by name. The `model.generate` span records `params` (values only — a schema is reported as its kind, never its content, kept by `TestParamsOnSpan` and the prompts-never-in-spans policy) and `params_forwarded`. The console surfaces `agent_role` as a chip on the requests table, the trace list and the span header. Tests: `TestParamsReachParamBackend`, `TestParamsOnSpan`, `TestClientRefOnSpans`, `TestOllamaForwardsParams`, `TestOpenAIForwardsParams`, `TestExtraLocalModelExplicit`.
- `docs/RULES.md` §12 *Console UI — the Tower design system*: eight rules for anything the console renders (tokens only, `tower-*` classes defined before use, reuse the catalogue, pill vs chip, one accent, four states per page, `h()` not `innerHTML`, document on the design-system page). Two are executable: `TestPolicyConsoleColoursAreTokens` (no hex outside `:root`) and `TestPolicyConsoleClassesExist` (every `tower-*` name the console uses is in `tower.css`) — both bind-checked against a scratch breakage. New root `AGENTS.md` (what a coding agent reads first: the rule book, the design system, the traps) and `CLAUDE.md` that imports it, so the UI rules are actually loaded rather than sitting in `docs/`. `AGENTS.md` is tool-neutral — OpenCode and Codex read it natively — and thin pointers cover the tools that don't: `GEMINI.md`, `opencode.json` (`instructions`), `.cursor/rules/agents.mdc`, `.github/copilot-instructions.md`; `TestPolicyAgentFilesPointHere` keeps them pointing at it and stops `CLAUDE.md` growing rules of its own.
- `docs/tower-design-system.html` rewritten for the shipped console: it now links `console/static/tower.css` instead of carrying a stale copy (the old page still showed the 64 px icon rail from the MVP) and documents every component the four pages use — shell, strip, primitives, feedback states, data display, both charts, the trace inspector — with a spec table per section and the rules for adding a component. New `docs/tower-enhancements.html`: a 20-example UI/UX study, each with what the console does today (line cited), the proposal, a live demo in Tower's own CSS, and impact/effort; three are accessibility defects (no focus rings, `ink-faint` at 2.6:1 on text, no reduced-motion guard), nine are quick wins, grouped into three shippable sprints.
- **Security / SonarCloud:** the quality gate failed on *Security Rating E on new code* — the hard-coded local-dev DSN `postgres://<user>:<password>@localhost…` in `main.go` trips Sonar's PostgreSQL-password secret detector (blocker). `defaultDSN()` in `config.go` now assembles it from `POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_HOST` / `POSTGRES_PORT` / `POSTGRES_DB` (same defaults as compose, passwords URL-escaped); the same literal is scrubbed from `docs/API.md`, `mcp/README.md` and lesson 05. The console's unused `html:` attribute (an `innerHTML` sink) is removed from the `h()` helper, and the Grafana admin password in compose is `${GRAFANA_ADMIN_PASSWORD:-admin}`. The env-vars-documented policy now also scans `envOr(...)` reads. Second round from the actual Sonar issue list: the compose `app` service passes `POSTGRES_HOST/USER/PASSWORD/DB` instead of a `POSTGRES_DSN` URL (the `${VAR:-default}` form still matched the secret detector), and every `uses:` in `ci.yml` is pinned to a full commit SHA with the release tag as a trailing comment (Sonar *Use full commit SHA hash for this dependency*; Dependabot keeps the pins bumped).
- Console shell redesign: 216 px labelled sidebar (brand, Operate / Quality / Links sections, Metrics + Models links, router status + version in the footer; collapses to icons under 900 px), sticky page header with the page title and live strip, slim themed scrollbars, content capped at 1480 px on wide screens, table/panel hover states, display-font metric values. Traces page opens the latest trace automatically and orders spans by tree depth.
- Console: the Traces page is now a real inspector — recent traces list (chats + workflows) with All/Chats/Workflows/Errors/Sensitive/Fallbacks filters and relative times, a waterfall timeline per trace, an indented span tree with expandable attributes, Copy-id / JSON actions, deep links that keep the list, a proper empty state; the status strip refreshes on every page. Spans now carry `started_at` in JSON; tracker steps record `latency_s` + `attempts` in their span (spans are emitted at completion, so the timeline reconstructs start = started_at − latency).
- **Security:** locality is now a property of the model, not only the backend — Ollama's hosted `name:cloud` models (served through the local API since 2026) are non-local, and `Plan()` removes *every* non-local candidate from a sensitive request's chain, primary tier included (previously only fallbacks were filtered). Found by the market study (`docs/MARKET.md`). Tests: `TestCloudSuffixModelIsNotLocal`, policy case.
- `docs/MARKET.md`: market study (Sep 2026) — landscape after the TensorZero archive and the Langfuse/Helicone/Promptfoo acquisitions, feature matrix, positioning ("the LLM-ops layer for Ollama"), P0/P1/P2 roadmap, success measures.
- CI hardening: per-ref concurrency with cancel-in-progress, job timeouts, least-privilege permissions, `GOTOOLCHAIN=local`, Go cache; Actions bumped (checkout v7, setup-go v7, buildx v4, build-push v7, gh-release v3) and `golang:1.27-alpine` base — the six open Dependabot PRs folded into one commit; Dependabot now groups updates per ecosystem (one PR per week each) so they stop conflicting on `ci.yml`.
- `docs/RULES.md`: the engineering rule set (problem fit, what we optimize for, success measures, architecture style, sync vs async, retries, latency, scalability, security, guardrails, testing/experiments, static analysis).
- `policy/` package: 11 executable policies (single direct dependency, prompts never in spans, sensitive never cloud, one error shape, non-blocking span sink, env vars and flags documented, contiguous migrations, ADRs indexed, no same-backend retry, rules cite real tests). Found two undocumented flags on its first run.
- CI `policy` job: `staticcheck` (v0.8.1), `govulncheck`, policy tests. `govulncheck` found GO-2026-5970 in `golang.org/x/text` (indirect via pgx) — bumped to v0.39.0.
- Documentation pass: README rewrite, CHANGELOG, `docs/adr/0001–0008`, PRIVACY rewrite, `docs/API.md` env reference, MCP README, lessons reconciled, corpus v3 (17 docs → 36 chunks) with golden v3 (72 pairs, frozen `26664d4d…`) scored **0.972** — the two misses are retrieval misses (recall 0), not unfaithful answers; v3 is now the default golden version.

## v1.0.0 — 2026-09-15

The MVP becomes a product: an installable gateway with a console.

### Track A — ops foundation
- `Dockerfile` (multi-stage, distroless nonroot, 21.7 MB) and a compose `app` profile that runs the router against the host's Ollama.
- GitHub Actions: gofmt · vet · `test -race` · build · docker build on every push; release job on `v*` tags builds linux/windows/darwin binaries + `SHA256SUMS`.
- `schema_migrations` tracking: `--migrate` applies each `migrations/*.sql` once and reports the count.
- Dependabot for Go modules, Docker base images and Actions. `--version` flag stamped at release.

### Track B — gateway hardening
- **OpenAI-compatible** `POST /v1/chat/completions`: `messages[]` in, `chat.completion` out, with `text/reason/backend/trace_id/fallback` extensions. The MVP `{prompt}` body still works.
- **Streaming** (`stream: true`) as SSE in the OpenAI chunk shape; usage in the final chunk.
- **Backends**: `Backend` interface; Ollama moved to `/api/chat` with NDJSON streaming; `OpenAIBackend` for Groq or any OpenAI-style API; 15 s health prober → `router_backend_up{backend}`; `GET /v1/models`.
- **Virtual API keys** (`0003_api_keys.sql`, `--create-key NAME --key-rpm N --key-budget T`): Bearer auth, per-key requests-per-minute, lifetime token budget, 401/403/429 in the one error shape, per-key counters.
- **Fail-closed rule**: sensitive requests (header, body flag, keyword) never get a cloud candidate; explicit cloud model → `403 sensitive_cloud_blocked`.
- Graceful shutdown (drain in-flight chats, flush spans), `REQUEST_TIMEOUT` → 504, `X-Trace-ID` on every response, `X-Request-ID` echo.

### Track C — Tower console
- Web UI served from the binary at `/` (embedded HTML/CSS/JS, no framework, no build step): overview (KPIs, routing traffic, backend health, recent requests), trace inspector, evals & drift (score history, worst cases, *Run eval suite*), workflows (start/resume).
- Console endpoints: `/v1/overview`, `/v1/requests`, `/v1/evals/{runs,status,run}`, `/v1/workflows` (+ `/{id}/resume`).
- Hardening: abortable fetches, one retry on 5xx, inline error + Retry on store failure, in-place polling.

### Fixes and learnings recorded along the way
- Prometheus histogram was double-cumulated (p99 would have been wrong). Fixed + test.
- Recall could exceed 1 when a doc's two chunks were both retrieved. Fixed + test.
- Local judge sampled at default temperature; pinned to 0. Fixed + test.
- Golden answers must be standalone main-clause sentences (one-word, list, and "Because…" answers score 0). `--freeze-golden` now rejects them.
- Eval run budget is `EVAL_TIMEOUT` (default 90 min); evals and live traffic contend for the 8 GB GPU (`docs/VRAM.md`).

## v0.1.0 — 2026-09-15

The MVP as shipped (six checkboxes; see the plan §1): router between two local Ollama models with a written rule and per-request reason; hand-written Prometheus metrics + Grafana dashboard (k6 gate: 0/2725 failed at 50 RPS, p99 70 ms); MCP server with 5 tools; fresh RAG corpus + reviewed golden set + hand-written faithfulness scorer (golden v2 = 1.000); durable tracker that survives `kill -9`; trace view over `trace_id/span_id/parent_id`.
